<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Docker Image Comparison</title>
    <style>
        :root {
            --bg: #0d1117;
            --surface: #161b22;
            --border: #30363d;
            --text: #c9d1d9;
            --text-muted: #8b949e;
            --accent: #58a6ff;
            --green: #3fb950;
            --red: #f85149;
            --orange: #d29922;
            --blue: #58a6ff;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif;
            background: var(--bg);
            color: var(--text);
            line-height: 1.6;
            padding: 2rem;
        }
        .dashboard { max-width: 1400px; margin: 0 auto; }
        h1 { color: #f0f6fc; margin-bottom: 0.5rem; font-size: 1.8rem; }
        h2 { color: #f0f6fc; margin: 1.5rem 0 0.75rem; font-size: 1.3rem; border-bottom: 1px solid var(--border); padding-bottom: 0.5rem; }
        h3 { color: var(--text); margin: 1rem 0 0.5rem; font-size: 1.1rem; }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 0.75rem 0;
            font-size: 0.9rem;
        }
        th {
            background: var(--surface);
            color: var(--text-muted);
            text-align: left;
            padding: 0.5rem 0.75rem;
            border: 1px solid var(--border);
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.75rem;
            letter-spacing: 0.05em;
        }
        td {
            padding: 0.5rem 0.75rem;
            border: 1px solid var(--border);
        }
        tr:hover td { background: rgba(177, 186, 196, 0.04); }
        code {
            background: rgba(110, 118, 129, 0.15);
            padding: 0.15rem 0.4rem;
            border-radius: 4px;
            font-size: 0.85em;
            font-family: 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, monospace;
        }
        .severity-critical { color: var(--red); font-weight: 700; }
        .severity-high { color: var(--orange); font-weight: 600; }
        .severity-medium { color: var(--orange); }
        .severity-low { color: var(--blue); }
        a { color: var(--accent); text-decoration: none; }
        a:hover { text-decoration: underline; }
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1rem; margin: 1rem 0; }
        .card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 6px;
            padding: 1rem;
        }
        .card-header {
            font-size: 1.1rem;
            font-weight: 600;
            color: #f0f6fc;
            margin-bottom: 0.75rem;
            padding-bottom: 0.5rem;
            border-bottom: 1px solid var(--border);
        }
        .card-label { color: var(--text-muted); font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.05em; }
        .card-value { font-size: 1.2rem; font-weight: 700; color: #f0f6fc; margin-top: 0.15rem; }
        .stat-row { display: flex; justify-content: space-between; padding: 0.35rem 0; border-bottom: 1px solid var(--border); }
        .stat-row:last-child { border-bottom: none; }
        .vuln-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 0.5rem; margin: 0.5rem 0; }
        .vuln-card { text-align: center; padding: 0.5rem; background: var(--bg); border-radius: 4px; }
        .vuln-count { font-size: 1.3rem; font-weight: 700; }
        .vuln-label { font-size: 0.7rem; color: var(--text-muted); margin-top: 0.15rem; }
        .best { color: var(--green); }
        .worst { color: var(--red); }
        .tab-container { margin: 1rem 0; }
        .tab-buttons { display: flex; gap: 0; border-bottom: 1px solid var(--border); margin-bottom: 0; }
        .tab-btn {
            padding: 0.5rem 1rem;
            background: none;
            border: 1px solid transparent;
            border-bottom: none;
            color: var(--text-muted);
            cursor: pointer;
            font-size: 0.85rem;
            font-family: inherit;
            border-radius: 6px 6px 0 0;
        }
        .tab-btn:hover { color: var(--text); }
        .tab-btn.active {
            background: var(--surface);
            color: #f0f6fc;
            border-color: var(--border);
            border-bottom: 1px solid var(--surface);
            margin-bottom: -1px;
        }
        .tab-content { display: none; background: var(--surface); border: 1px solid var(--border); border-top: none; border-radius: 0 0 6px 6px; padding: 1rem; }
        .tab-content.active { display: block; }
        .footer { margin-top: 2rem; padding-top: 1rem; border-top: 1px solid var(--border); color: var(--text-muted); font-size: 0.8rem; }
    </style>
</head>
<body>
    <div class="dashboard">
        <h1>Docker Image Comparison</h1>
        <p style="color: var(--text-muted);">Comparing {{ len .Images }} image tags</p>

        <h2>Overview</h2>
        <table>
            <thead>
                <tr>
                    <th>Tag</th>
                    <th>Size</th>
                    <th>Layers</th>
                    <th>Efficiency</th>
                    <th>Critical</th>
                    <th>High</th>
                    <th>Medium</th>
                    <th>Low</th>
                    <th>Total Vulns</th>
                    <th>Packages</th>
                </tr>
            </thead>
            <tbody>
                {{- range .Images }}
                <tr>
                    <td><code>{{ .ImageTag }}</code></td>
                    <td>{{ .SizeMB }}</td>
                    <td>{{ .TotalLayers }}</td>
                    <td>{{ printf "%.1f" .Efficiency }}%</td>
                    <td>{{ if gt (index .VulnSummary "Critical") 0 }}<span class="severity-critical">{{ index .VulnSummary "Critical" }}</span>{{ else }}<span class="best">0</span>{{ end }}</td>
                    <td>{{ if gt (index .VulnSummary "High") 0 }}<span class="severity-high">{{ index .VulnSummary "High" }}</span>{{ else }}<span class="best">0</span>{{ end }}</td>
                    <td>{{ if gt (index .VulnSummary "Medium") 0 }}<span class="severity-medium">{{ index .VulnSummary "Medium" }}</span>{{ else }}0{{ end }}</td>
                    <td>{{ if gt (index .VulnSummary "Low") 0 }}<span class="severity-low">{{ index .VulnSummary "Low" }}</span>{{ else }}0{{ end }}</td>
                    <td>{{ .TotalVulns }}</td>
                    <td>{{ .TotalPackages }}</td>
                </tr>
                {{- end }}
            </tbody>
        </table>

        <h2>Per-Image Details</h2>
        <div class="tab-container">
            <div class="tab-buttons">
                {{- range $i, $img := .Images }}
                <button class="tab-btn{{ if eq $i 0 }} active{{ end }}" onclick="switchTab(event, 'tab-{{ $i }}')">{{ $img.ImageTag }}</button>
                {{- end }}
            </div>
            {{- range $i, $img := .Images }}
            <div id="tab-{{ $i }}" class="tab-content{{ if eq $i 0 }} active{{ end }}">
                <div class="grid" style="grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));">
                    <div class="card">
                        <div class="card-label">Image Size</div>
                        <div class="card-value">{{ $img.SizeMB }}</div>
                    </div>
                    <div class="card">
                        <div class="card-label">Layers</div>
                        <div class="card-value">{{ $img.TotalLayers }}</div>
                    </div>
                    <div class="card">
                        <div class="card-label">Efficiency</div>
                        <div class="card-value">{{ printf "%.1f" $img.Efficiency }}%</div>
                    </div>
                    <div class="card">
                        <div class="card-label">Packages</div>
                        <div class="card-value">{{ $img.TotalPackages }}</div>
                    </div>
                </div>

                <h3>Image Details</h3>
                <table>
                    <tbody>
                        <tr><td><strong>Base Image OS</strong></td><td>{{ if $img.OSDistro }}{{ $img.OSDistro }} ({{ $img.OS }}/{{ $img.Architecture }}){{ else }}{{ $img.OS }} ({{ $img.Architecture }}){{ end }}</td></tr>
                        {{- if $img.SupportedArchitectures }}
                        <tr><td><strong>Supported Architectures</strong></td><td>{{ join $img.SupportedArchitectures ", " }}</td></tr>
                        {{- end }}
                        <tr><td><strong>Wasted Space</strong></td><td>{{ $img.WastedBytes }}</td></tr>
                    </tbody>
                </table>

                <h3>Vulnerabilities</h3>
                {{- if not $img.VulnScanTime.IsZero }}
                <p style="color: var(--text-muted); font-size: 0.85rem;">Last scanned: {{ $img.VulnScanTime.Format "2006-01-02T15:04:05Z07:00" }}</p>
                {{- end }}

                <div class="vuln-grid">
                    <div class="card vuln-card">
                        <div class="vuln-count severity-critical">{{ index $img.VulnSummary "Critical" }}</div>
                        <div class="vuln-label">Critical</div>
                    </div>
                    <div class="card vuln-card">
                        <div class="vuln-count severity-high">{{ index $img.VulnSummary "High" }}</div>
                        <div class="vuln-label">High</div>
                    </div>
                    <div class="card vuln-card">
                        <div class="vuln-count severity-medium">{{ index $img.VulnSummary "Medium" }}</div>
                        <div class="vuln-label">Medium</div>
                    </div>
                    <div class="card vuln-card">
                        <div class="vuln-count severity-low">{{ index $img.VulnSummary "Low" }}</div>
                        <div class="vuln-label">Low</div>
                    </div>
                </div>

                {{- if $img.Vulnerabilities }}
                <details>
                    <summary style="cursor: pointer; color: var(--accent); margin: 0.5rem 0;">Show all {{ $img.TotalVulns }} vulnerabilities</summary>
                    <table>
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Severity</th>
                                <th>Package</th>
                                <th>Version</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{- range $img.Vulnerabilities }}
                            <tr>
                                <td><a href="https://nvd.nist.gov/vuln/detail/{{ .ID }}">{{ .ID }}</a></td>
                                <td><span class="severity-{{ lower .Severity }}">{{ .Severity }}</span></td>
                                <td><code>{{ .Package }}</code></td>
                                <td><code>{{ .Version }}</code></td>
                            </tr>
                            {{- end }}
                        </tbody>
                    </table>
                </details>
                {{- end }}

                {{- if $img.Packages }}
                <details>
                    <summary style="cursor: pointer; color: var(--accent); margin: 0.5rem 0;">Show all {{ $img.TotalPackages }} packages</summary>
                    <table>
                        <thead>
                            <tr>
                                <th>Package</th>
                                <th>Version</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{- range $img.Packages }}
                            <tr>
                                <td>{{ .Name }}</td>
                                <td>{{ .Version }}</td>
                            </tr>
                            {{- end }}
                        </tbody>
                    </table>
                </details>
                {{- end }}
            </div>
            {{- end }}
        </div>

        <div class="footer">
            Generated by <a href="https://github.com/northcutted/dock-docs">dock-docs</a>
        </div>
    </div>

    <script>
    function switchTab(evt, tabId) {
        document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));
        document.querySelectorAll('.tab-btn').forEach(el => el.classList.remove('active'));
        document.getElementById(tabId).classList.add('active');
        evt.currentTarget.classList.add('active');
    }
    </script>
</body>
</html>
